// APG Interaction Reliability v37 remains the governing consumer runtime.
// PageSpeed Certification v30 remains the underlying accessibility/performance layer.
// Vercel Analytics v38 remains the privacy-hardened telemetry layer.
// Amazon shopping discovery v39 remains the governed shopping shell.
// Brand System v46 is the final presentation layer so every route and breakpoint
// follows the current APG visual authority without changing recommendation logic.
require('../lib/scout-concierge-v5-runtime');
module.exports=require('../lib/brand-system-v46');

'use strict';
const {products,categories}=require('../data');
const images=require('../data/product-images');
const amazon=require('../data/retailers-v6');
const retailerVerifications=require('../data/retailer-verifications-v109');
const VERSION='catalogue-intelligence-v48',PROFILE_SCHEMA='apg-product-intelligence-profile-v1',COMMERCIAL_RECOMMENDATION_WEIGHT=0;
const PRODUCT_BY_SLUG=new Map(products.map(p=>[p.slug,p]));
const uniq=xs=>[...new Set((xs||[]).filter(Boolean))],clean=s=>String(s||'').trim();

// Historical/certified v48 commerce counts intentionally remain based on canonical
// exactModel product.offers so Action 5's independently certified recount stays stable.
function currentOffer(o){if(!o||o.exactModel!==true||!o.url||!o.retailer)return false;const due=clean(o.reviewDue),today=new Date().toISOString().slice(0,10);return !due||due>=today;}
function exactOffers(p){return(p.offers||[]).filter(x=>x&&x.exactModel===true&&x.url&&x.retailer);}

// Customer-facing retailer truth is a later, richer view used by the current v112
// retailer surface. It combines maintained `retailers + offers` and then applies any
// narrowly refreshed exact-model verification record. This does not rewrite the
// historical Action 5/canonical-offer census or recommendation weight.
function customerRetailerRows(p){
 const rows=[...(Array.isArray(p?.retailers)?p.retailers:[]),...(Array.isArray(p?.offers)?p.offers:[])].map(row=>retailerVerifications.resolve(p?.slug,row)),seen=new Set();
 return rows.filter(row=>{if(!row||!row.retailer)return false;const href=row.affiliateUrl||row.url;if(!href)return false;const key=`${row.retailer}|${href}`;if(seen.has(key))return false;seen.add(key);return true;});
}
function customerExactDestination(row){
 if(!row||!row.retailer||!(row.url||row.affiliateUrl))return false;
 const status=clean(row.amazonMatchStatus).toUpperCase(),model=clean(row.amazonModelMatch).toLowerCase(),productMatch=clean(row.productMatch).toLowerCase();
 return status==='EXACT_VERIFIED'||model==='exact'||(row.verified===true&&productMatch==='exact')||row.exactModel===true;
}
function currentCustomerDestination(row){
 if(!customerExactDestination(row))return false;
 const due=clean(row.reviewDue),today=new Date().toISOString().slice(0,10);
 return !due||due>=today;
}
function customerRetailerTruth(p){
 const exact=customerRetailerRows(p).filter(customerExactDestination),current=exact.filter(currentCustomerDestination);
 return {exactAustralianDestinationCount:exact.length,currentExactAustralianDestinationCount:current.length,retailers:uniq(current.map(x=>x.retailer))};
}
function structuredSpecs(p){const out={};for(const row of p.specs||[]){if(!Array.isArray(row)||row.length<2)continue;const key=clean(row[0]).toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');if(key)out[key]=String(row[1]);}return out;}
function evidenceLevel(p){const facts=Object.keys(p.factEvidence||{}).length,specs=(p.specs||[]).length;if(facts>0)return 'fact-verified';if(specs>0&&p.source)return 'structured-maintained';return 'classification-maintained';}
function evidenceConfidence(p){const level=evidenceLevel(p);if(level==='fact-verified'&&p.freshnessStatus==='reviewed-this-month')return 'high';if(level==='fact-verified'||level==='structured-maintained')return 'moderate';return 'limited';}
function imageState(p){const record=images.imageFor(p),errors=record?images.validationErrors(p,record):[];if(record?.imageStatus==='verified'&&record.imageVerified&&errors.length===0)return{status:'verified-authorised-photography',verified:true,rightsVerified:true,sourceType:record.imageSourceType||null,checkedAt:record.imageVerifiedAt||null};if(amazon.direct?.[p.slug])return{status:'awaiting-approved-image-delivery',verified:false,rightsVerified:false,sourceType:null,checkedAt:p.lastImageVerification||null};return{status:'authorised-exact-product-source-needed',verified:false,rightsVerified:false,sourceType:null,checkedAt:p.lastImageVerification||null};}
function profileFor(input){
 const p=typeof input==='string'?PRODUCT_BY_SLUG.get(input):input;if(!p)return null;const c=categories[p.category]||{},facts=p.factEvidence||{},offers=exactOffers(p),current=offers.filter(currentOffer),customerRetailer=customerRetailerTruth(p),photo=imageState(p),tags=uniq(p.tags||[]),priorities=uniq(c.priorities||[]),matchedPriorities=priorities.filter(x=>tags.includes(x)),specs=structuredSpecs(p),level=evidenceLevel(p),gaps=[],nextActions=[];
 if(!Object.keys(facts).length){gaps.push('No fact-level evidence map yet');nextActions.push('Verify decision-relevant facts against manufacturer/manual or other credible exact-model evidence');}
 if(!Object.keys(specs).length){gaps.push('No structured specification set yet');nextActions.push('Capture decision-relevant exact-model specifications with provenance');}
 if(!current.length){gaps.push('No current exact-model Australian retailer destination yet');nextActions.push('Verify at least one exact Australian retailer or manufacturer-direct destination');}
 if(!photo.verified){gaps.push('Genuine product photography awaiting an authorised exact-product source');nextActions.push('Acquire genuine photography only through an authorised exact-product source and rights review');}
 return{schemaVersion:PROFILE_SCHEMA,intelligenceVersion:VERSION,identity:{id:p.id||p.productId||p.slug,slug:p.slug,brand:p.brand,name:p.name,model:p.model||null,category:p.category,categoryLabel:p.categoryLabel||c.label||p.category},decision:{categoryFactors:uniq(c.factors||[]),categoryPriorities:priorities,maintainedClassificationSignals:tags,matchedCategoryPriorities:matchedPriorities,structuredAttributes:{...(p.decisionAttributes||{})},signalBasis:'Maintained APG catalogue classification unless separately backed by factEvidence.',commercialRecommendationWeight:0},evidence:{level,tier:p.evidenceTier||'starter',confidence:evidenceConfidence(p),testingStatus:p.testingStatus||'Desk-researched / maintained catalogue record',source:p.source||null,sourceType:p.sourceType||null,structuredSpecCount:Object.keys(specs).length,structuredSpecifications:specs,verifiedFactCount:Object.keys(facts).length,verifiedFacts:facts,lastSourceVerification:p.lastSourceVerification||null,lastSubstantiveReview:p.lastSubstantiveReview||p.lastReviewed||null,nextReviewDue:p.nextReviewDue||null,gaps,policy:'Classification and specification context can inform discovery; only separately maintained factEvidence is represented as fact-level verification.'},commerce:{exactAustralianDestinationCount:offers.length,currentExactAustralianDestinationCount:current.length,retailers:uniq(current.map(x=>x.retailer)),customerCurrentExactAustralianDestinationCount:customerRetailer.currentExactAustralianDestinationCount,customerExactAustralianDestinationCount:customerRetailer.exactAustralianDestinationCount,customerRetailers:customerRetailer.retailers,lastRetailerCheck:p.lastRetailerCheck||null,lastPriceCheck:p.lastPriceCheck||null,priceBasis:Number(p.price)>0?Number(p.price):null,priceStatus:Number(p.price)>0?'maintained-price-basis':'live-price-not-maintained',commercialRecommendationWeight:0,policy:'Certified historical commerce counts remain based on canonical exact offers. The customer retailer view additionally reflects maintained exact retailer rows plus narrowly refreshed exact-model verifications used by the current shopping surface. Retailer participation, affiliate status, price-feed availability and commission contribute zero suitability points.'},imagery:photo,operations:{needsFactVerification:!Object.keys(facts).length,needsStructuredSpecifications:!Object.keys(specs).length,needsExactAustralianRetailer:!current.length,needsAuthorisedPhotography:!photo.verified,nextActions},completeness:{contractComplete:true,contractFields:['identity','decision','evidence','commerce','imagery','operations'],evidenceDepthIndependentOfContract:true}};
}
function categorySummary(slug){const rows=categories[slug]?.products||[],profiles=rows.map(profileFor).filter(Boolean);return{slug,label:categories[slug]?.label||slug,products:rows.length,profiles:profiles.length,contractCoveragePct:rows.length?Math.round(profiles.length/rows.length*1000)/10:100,factVerified:profiles.filter(x=>x.evidence.level==='fact-verified').length,structuredMaintained:profiles.filter(x=>x.evidence.level==='structured-maintained').length,classificationMaintained:profiles.filter(x=>x.evidence.level==='classification-maintained').length,withCurrentExactRetailer:profiles.filter(x=>x.commerce.currentExactAustralianDestinationCount>0).length,withVerifiedPhotography:profiles.filter(x=>x.imagery.verified).length};}
function summary(){const profiles=products.map(profileFor).filter(Boolean),cats=Object.keys(categories).map(categorySummary);return{version:VERSION,schemaVersion:PROFILE_SCHEMA,catalogue:{products:products.length,categories:Object.keys(categories).length,profiles:profiles.length,contractCoveragePct:products.length?Math.round(profiles.length/products.length*1000)/10:100},evidence:{factVerified:profiles.filter(x=>x.evidence.level==='fact-verified').length,structuredMaintained:profiles.filter(x=>x.evidence.level==='structured-maintained').length,classificationMaintained:profiles.filter(x=>x.evidence.level==='classification-maintained').length},commerce:{productsWithCurrentExactAustralianDestination:profiles.filter(x=>x.commerce.currentExactAustralianDestinationCount>0).length,exactAustralianDestinationCount:profiles.reduce((n,x)=>n+x.commerce.exactAustralianDestinationCount,0),commercialRecommendationWeight:0},imagery:{productsWithVerifiedPhotography:profiles.filter(x=>x.imagery.verified).length,productsAwaitingAuthorisedPhotography:profiles.filter(x=>!x.imagery.verified).length},researchBacklog:{needsFactVerification:profiles.filter(x=>x.operations.needsFactVerification).length,needsStructuredSpecifications:profiles.filter(x=>x.operations.needsStructuredSpecifications).length,needsExactAustralianRetailer:profiles.filter(x=>x.operations.needsExactAustralianRetailer).length,needsAuthorisedPhotography:profiles.filter(x=>x.operations.needsAuthorisedPhotography).length,policy:'Backlog counts are evidence gaps, not product-quality or popularity scores.'},categories:cats,governance:{uniformContract:true,equalEvidenceClaim:false,productionSelfModification:false,commercialRecommendationWeight:0}};}
module.exports={VERSION,PROFILE_SCHEMA,COMMERCIAL_RECOMMENDATION_WEIGHT,profileFor,categorySummary,summary,currentOffer,evidenceLevel,exactOffers,customerRetailerRows,customerExactDestination,currentCustomerDestination,customerRetailerTruth};

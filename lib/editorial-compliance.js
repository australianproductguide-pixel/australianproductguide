const app=require('./illustrative-experience');
const {categories,products}=require('../data');
const {brands,pairPages,indexableRoutes}=require('./routes');

const REVIEW_DATE='17 August 2026';
const AMAZON_STATEMENT='As an Amazon Associate I earn from qualifying purchases.';
const PRODUCT_COUNT=products.length,CATEGORY_COUNT=Object.keys(categories).length,BRAND_COUNT=brands.length,COMPARISON_COUNT=pairPages.length,ROUTE_COUNT=indexableRoutes.length;
const PRODUCT_BY_SLUG=new Map(products.map(p=>[p.slug,p]));
function fmt(v){if(!v)return '';const m=String(v).match(/^(\d{4})-(\d{2})-(\d{2})$/);if(!m)return String(v);const ms=['January','February','March','April','May','June','July','August','September','October','November','December'];return `${Number(m[3])} ${ms[Number(m[2])-1]} ${m[1]}`;}
function category(path){let m=path.match(/^\/categories\/([^/]+)(?:\/finder)?\/$/);if(m)return categories[m[1]];m=path.match(/^\/guides\/([^/]+)-buying-guide\/$/);return m?categories[m[1]]:null;}
function product(path){const m=path.match(/^\/products\/([^/]+)\/$/);return m?PRODUCT_BY_SLUG.get(m[1]):null;}
function categoryTier(c){if(!c)return 'Evidence depth varies by product';const tiers=new Set(c.products.map(p=>p.evidenceTier||c.evidenceTier||'starter'));if(tiers.size>1)return 'Evidence depth varies by product';const t=[...tiers][0];return t==='deep'?'Deep desk research':t==='manufacturer-verified'?'Manufacturer-verified model evidence':'Starter evidence';}
function categoryEvidence(c){const starter=c.products.filter(p=>(p.evidenceTier||c.evidenceTier)==='starter').length;if(starter===c.products.length)return `This category currently contains ${c.products.length} maintained starter-evidence records. Product identity and discovery sources are disclosed, while deeper Australian manufacturer/specification verification remains in progress. Verify the exact model, specifications, warranty, seller, price and availability before purchase.`;if(starter)return `This category contains ${c.products.length} maintained records with mixed evidence depth. ${starter} currently use starter evidence; each product page discloses its source and verification status. Commercial relationships contribute zero recommendation points.`;return `This category contains ${c.products.length} maintained records with source, testing status and freshness disclosed on each product page. Commercial relationships contribute zero recommendation points.`;}
function globalCopy(out){return String(out||'')
 .replace(/As an Amazon Associate,?\s*Australian Product Guide may earn from qualifying purchases\./g,AMAZON_STATEMENT)
 .replace(/As an Amazon Associate,?\s*APG may earn from qualifying purchases\./g,AMAZON_STATEMENT)
 .replace(/<span class="pill good">Effective 16 August 2026<\/span><span class="pill">Last updated 16 August 2026<\/span>/g,'<span class="pill good">Effective 17 August 2026</span><span class="pill">Last updated 17 August 2026</span>')
 .replace(/37 products across four categories/g,`${PRODUCT_COUNT} maintained products across ${CATEGORY_COUNT} populated categories`)
 .replace(/37-product/g,`${PRODUCT_COUNT}-product`).replace(/four live categories/g,`${CATEGORY_COUNT} populated categories`)
 .replace(/16 maintained brands/g,`${BRAND_COUNT} maintained brands`).replace(/16 brands represented/g,`${BRAND_COUNT} brands represented`)
 .replace(/139 canonical research routes/g,`${ROUTE_COUNT} canonical research routes`).replace(/56 prepared head-to-heads/g,`${COMPARISON_COUNT} prepared head-to-heads`)
 .replace(/Best-value alternative/g,'Value-focused alternative')
 .replace(/<p class="eyebrow">Best match for your answers<\/p>/g,'<p class="eyebrow">Closest match in APG’s maintained set</p>')
 .replace(/Fit score is a shortlist signal, not a performance rating\./g,'Fit score is a shortlist signal within APG’s maintained set, not a performance rating or a whole-of-market claim.');}
function reviewCategory(out,c){if(!c)return out;const label=categoryTier(c),copy=categoryEvidence(c);return out
 .replace(/<span class="independence-badge">Reviewed 15 Aug 2026<\/span>/g,`<span class="independence-badge">${label}</span>`)
 .replace(/<p>This category contains \d+ maintained records reviewed against primary manufacturer evidence\. Commercial relationships contribute zero recommendation points\.<\/p>/g,`<p>${copy}</p>`)
 .replace(/<h3>\d+ maintained products<\/h3><p>Reviewed against primary manufacturer evidence\.<\/p>/g,`<h3>${c.products.length} maintained products</h3><p>${copy}</p>`)
 .replace(/<strong>\d+ maintained products<\/strong><span>Evidence \+ comparison \+ finder \+ retailer pathways<\/span>/g,`<strong>${c.products.length} maintained products</strong><span>${label} · comparison · finder · retailer pathways</span>`);}
function reviewProduct(out,p){if(!p)return out;const date=fmt(p.lastSourceVerification||p.lastSubstantiveReview||p.firstResearched||p.lastReviewed)||REVIEW_DATE,label=p.evidenceTier==='starter'?'Source identity checked':'Evidence checked';out=out.replace(/<span>Reviewed 15 Aug 2026<\/span>/g,`<span>${label} ${date}</span>`).replace(/<dt>Last reviewed<\/dt><dd>15 August 2026<\/dd>/g,`<dt>${label}</dt><dd>${date}</dd>`);if(p.evidenceTier==='starter')out=out.replace(/These are documented characteristics, not a claim of hands-on performance testing\./g,'These are starter-evidence decision signals, not hands-on performance findings. Confirm the exact Australian model and current specifications before purchase.').replace(/<p class="kicker">Why consider it<\/p><h2>Who this product is most likely to suit<\/h2>/g,'<p class="kicker">Research starting point</p><h2>Who this product may be worth investigating</h2>');return out;}
function privacy(out){const note=`<div class="notice apg-current-practice"><strong>Current data practices · ${REVIEW_DATE}.</strong> Core browsing, search, comparison and recommendation tools work without an account. My APG accounts are optional and can sync selected workspace data. Google Analytics is opt-in and is not loaded until a visitor allows analytics; advertising storage and personalisation remain off. APG also uses privacy-minimised first-party performance measurement. No account or analytics choice changes product suitability or retailer ranking.</div>`;if(!out.includes('apg-current-practice')){const a=out.indexOf('<p class="policy-lead">'),e=a<0?-1:out.indexOf('</p>',a);if(e>=0)out=out.slice(0,e+4)+note+out.slice(e+4);}return out.replace(/APG does not operate checkout, payment-card capture, newsletter signup or a public free-text contact form\./g,'APG does not operate checkout, payment-card capture or a public free-text contact form. Any product-research email preference is separate, optional and must use a valid consent process.');}
function affiliate(out){return out.replace(/<p><strong>As an Amazon Associate I earn from qualifying purchases\.<\/strong><\/p>/g,`<div class="notice"><strong>${AMAZON_STATEMENT}</strong> Amazon affiliate links are paid links and are labelled near the relevant action. Affiliate status, commission opportunity and retailer participation contribute zero points to suitability or recommendation scoring.</div>`);}
function coverage(out){return out.replace(/APG maintains \d+ products across [^.]+\. Maintained categories have structured product records, comparison routes, buying guidance and recommendation journeys\./g,`APG currently maintains ${PRODUCT_COUNT} products across ${CATEGORY_COUNT} populated categories. Coverage depth varies by category and product; evidence tier, source type and retailer verification are disclosed rather than presented as equivalent.`).replace(/A wider set of category pathways exists for future evaluation\. These remain noindex until APG can define the decision factors, establish an Australian evidence set and maintain changes responsibly\./g,'Published category pathways currently contain maintained catalogue records. New categories should only become indexable after APG can define the buying decision, maintain an Australian evidence set and support responsible freshness controls.');}
function updates(out){if(out.includes('aug17-editorial-review'))return out;const a=out.indexOf('<p class="policy-lead">'),e=a<0?-1:out.indexOf('</p>',a);if(e<0)return out;const section=`<h2 id="aug17-editorial-review">17 August 2026 — editorial, compliance and discovery review</h2><p>APG’s current catalogue, discovery architecture and trust content were reviewed against the live operating model. Site-wide copy now distinguishes starter evidence from deeper manufacturer-backed research, uses current evidence dates where available, strengthens Amazon paid-link disclosures, clarifies optional account and analytics practices, and avoids presenting the maintained catalogue as whole-of-market coverage.</p><p><strong>Current scope:</strong> ${PRODUCT_COUNT} maintained products, ${CATEGORY_COUNT} populated categories, ${BRAND_COUNT} represented brands and ${COMPARISON_COUNT} prepared head-to-head comparison routes. These figures are derived from the current structured catalogue rather than manually maintained marketing claims.</p>`;return (out.slice(0,e+4)+section+out.slice(e+4)).replace('<h2 id="phase4">16 August 2026 — Phase 4 premium discovery release</h2>','<h2 id="phase4">Historical · 16 August 2026 — Phase 4 premium discovery release</h2>');}
function contact(out){return out.replace(/Australian Product Guide currently keeps its public data footprint deliberately small\. There is no public account system, newsletter form or general-purpose contact form\./g,'Australian Product Guide keeps its public data footprint deliberately small. My APG accounts are optional; APG does not currently operate a general-purpose public free-text contact form.').replace(/APG currently collects very little identifiable user information in its own application\. If identifiable data collection is introduced, the public contact process will be updated alongside the <a href="\/privacy\/">Privacy Policy<\/a>\./g,'APG collects limited identifiable information when a shopper chooses to create an account or opts into a feature that requires contact details. The <a href="/privacy/">Privacy Policy</a> explains current collection, storage, analytics choices and account controls.');}
function transform(html,path){let out=globalCopy(html);out=reviewCategory(out,category(path));out=reviewProduct(out,product(path));if(path==='/privacy/')out=privacy(out);if(path==='/affiliate-disclosure/')out=affiliate(out);if(path==='/coverage/')out=coverage(out);if(path==='/updates/')out=updates(out);if(path==='/contact/')out=contact(out);return out;}
module.exports=(req,res)=>{let path='/';try{path=new URL(req.url,'https://australianproductguide.au').pathname}catch{}const end=res.end.bind(res);res.end=(body,...args)=>{const type=String(res.getHeader('Content-Type')||'').toLowerCase();if(req.method!=='HEAD'&&res.statusCode===200&&typeof body==='string'&&type.startsWith('text/html'))body=transform(body,path);return end(body,...args);};return app(req,res);};
module.exports.transform=transform;module.exports.AMAZON_STATEMENT=AMAZON_STATEMENT;

'use strict';

// Decision Lab resilience v50.4.
// Interactive Decision Lab submissions use a small isolated JSON contract rather
// than importing the fully transformed SSR page into the live document. The SSR
// route remains authoritative for direct/deep-link loads; the stable form shell
// remains mounted for interactive updates and every request is bounded.
const downstream=require('./catalogue-intelligence-v49-pass10-runtime');
const {publicDecision}=require('./decision-engine-v4');

const ASSET_PATH='/assets/decision-lab-resilience-v50.js';
const VERSION='50.4';
// Keep the established r4 marker so the governed Production browser diagnostic
// can exercise the new transport/rendering implementation without weakening its
// existing stable-shell assertions.
const PATCH='decision-lab-p0-2026-08-20-stable-shell-r4';
const ENGINE='decision-engine-v4';
const JSON_HEADER='x-apg-decision-json';

const clientJs=String.raw`
;(()=>{
if(window.__APG_DECISION_LAB_RESILIENCE_V50__)return;
window.__APG_DECISION_LAB_RESILIENCE_V50__='${PATCH}';
const DEADLINE_MS=10000;
const SLOW_MS=3500;
let activeController=null,deadlineTimer=0,slowTimer=0,requestSerial=0;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=v=>Number(v)>0?'A$'+Number(v).toLocaleString('en-AU'):'';
function isDecisionForm(form){return !!form&&location.pathname==='/decision-lab/'&&form.matches('form.decision-form[data-busy-form]')}
function buttonOf(form){return form?.querySelector('button[type="submit"],input[type="submit"]')||null}
function setDecisionState(state){if(document.body)document.body.dataset.apgDecisionV50State=state}
function statusOf(form){let s=form?.querySelector('[data-decision-submit-status]');if(s)return s;if(!form)return null;s=document.createElement('span');s.className='decision-submit-status';s.dataset.decisionSubmitStatus='';s.setAttribute('role','status');s.setAttribute('aria-live','polite');(form.querySelector('.decision-form-actions')||form).appendChild(s);return s}
function setStatus(form,message,error=false){const s=statusOf(form);if(!s)return;s.textContent=message||'';s.classList.toggle('is-error',!!error)}
function setBusy(form){setDecisionState('loading');form.dataset.apgDecisionV50Submitting='true';form.setAttribute('aria-busy','true');const b=buttonOf(form);if(b){if(!b.dataset.apgDecisionV50Label)b.dataset.apgDecisionV50Label=b.textContent||b.value||'Build my shortlist';b.disabled=true;b.setAttribute('aria-busy','true');if(b.tagName==='INPUT')b.value='Building shortlist…';else b.textContent='Building shortlist…'}setStatus(form,'Decision Engine v4 is matching your requirements against maintained APG product data.')}
function resetBusy(form){if(!form)return;delete form.dataset.apgDecisionV50Submitting;form.removeAttribute('aria-busy');const b=buttonOf(form);if(b){b.disabled=false;b.removeAttribute('aria-busy');const label=b.dataset.apgDecisionV50Label||b.dataset.old||'Build my shortlist';if(b.tagName==='INPUT')b.value=label;else b.textContent=label}}
function clearTimers(){if(deadlineTimer)clearTimeout(deadlineTimer);if(slowTimer)clearTimeout(slowTimer);deadlineTimer=slowTimer=0}
function safeTarget(form){const target=new URL('/decision-lab/',location.origin),params=new URLSearchParams();for(const [k,v] of new FormData(form).entries()){if(typeof v==='string'&&v.trim())params.append(k,v.trim())}target.search=params.toString();return target}
function hasInput(target){return ['q','category','budget','brand'].some(k=>String(target.searchParams.get(k)||'').trim())}
function syncFormFromTarget(form,target){for(const name of ['q','category','budget','brand']){const e=form?.elements.namedItem(name);if(e)e.value=target.searchParams.get(name)||''}}
function sendEvent(name,form){const d=form?new FormData(form):new FormData();const payload={event:name,engine:'${ENGINE}',interactionPatch:'${PATCH}',category:String(d.get('category')||'').slice(0,80),hasBudget:!!String(d.get('budget')||'').trim(),hasBrandPreference:!!String(d.get('brand')||'').trim(),viewport:innerWidth<600?'mobile':innerWidth<1024?'tablet':'desktop'};try{const blob=new Blob([JSON.stringify(payload)],{type:'application/json'});if(navigator.sendBeacon&&navigator.sendBeacon('/api/decision-telemetry',blob))return}catch{}try{fetch('/api/decision-telemetry',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload),keepalive:true}).catch(()=>{})}catch{}}
function syncLocalActions(){let saved=[],compare=[];try{const v=JSON.parse(localStorage.getItem('apgSaved')||'[]');if(Array.isArray(v))saved=v}catch{}try{const v=JSON.parse(localStorage.getItem('apgCompare')||'[]');if(Array.isArray(v))compare=v.slice(0,4)}catch{}document.querySelectorAll('[data-save-product]').forEach(b=>{const on=saved.includes(b.dataset.saveProduct);b.classList.toggle('saved',on);b.setAttribute('aria-pressed',String(on));b.textContent=on?'♥':'♡';b.title=on?'Remove from saved products':'Save product on this device'});document.querySelectorAll('[data-compare-product]').forEach(b=>{const on=compare.includes(b.dataset.compareProduct);b.classList.toggle('selected',on);b.classList.toggle('active',on);b.setAttribute('aria-pressed',String(on));b.textContent=on?'Added':'Compare'});const tray=document.getElementById('compareTray');if(tray){const count=tray.querySelector('[data-compare-count]');if(count)count.textContent=String(compare.length);const link=tray.querySelector('[data-compare-link]');if(link){link.href=compare.length?'/compare/custom/?products='+compare.map(encodeURIComponent).join(','):'/compare/custom/';link.classList.toggle('disabled',compare.length<2);link.setAttribute('aria-disabled',String(compare.length<2))}tray.hidden=compare.length===0}}
function removeOldOutcome(main,form){const section=form.closest('section');if(!section||section.parentElement!==main)throw new Error('missing_live_decision_shell');let n=section.nextSibling;while(n){const next=n.nextSibling;n.remove();n=next}return section}
function list(items,cls=''){if(!Array.isArray(items)||!items.length)return '';return '<ul'+(cls?' class="'+cls+'"':'')+'>'+items.map(x=>'<li>'+esc(x)+'</li>').join('')+'</ul>'}
function resultCard(r,i,clean){const conflict=[...(r.conflicts||[]),...(r.hardFailures||[]),...(r.verificationNeeds||[])];const reasons=list((r.reasons||[]).slice(0,5));const gaps=list((r.gaps||[]).slice(0,4));const conflicts=list(conflict.slice(0,5));const price=money(r.priceBasis);return '<article class="decision-result '+(i===0&&clean?'decision-result-top':'')+'"><div class="decision-rank"><span>'+(clean?(i===0?'Best fit':'Alternative '+(i+1)):'Closest maintained option')+'</span><strong class="decision-match">'+esc(r.match||'Alternative')+'</strong></div><div class="decision-result-grid"><div class="product-visual v7-semantic-product-visual"><div class="visual-copy"><span class="visual-brand">'+esc(r.brand)+'</span><strong>'+esc(r.name)+'</strong><small>'+esc(r.category||'Maintained APG product')+'</small></div></div><div class="decision-result-copy"><p class="eyebrow">'+esc(r.brand)+' · '+esc(r.category)+'</p><h2><a href="'+esc(r.url)+'">'+esc(r.name)+'</a></h2><div class="decision-confidence"><span class="pill">'+esc(r.confidence?.label||'Limited evidence')+'</span><span class="pill '+(r.hardConstraintStatus==='eligible'?'good':'')+'">'+esc(r.hardConstraintStatus||'unverified')+'</span></div>'+(reasons?'<div class="decision-reasons"><strong>Why it fits</strong>'+reasons+'</div>':'')+(conflicts?'<div class="decision-verification"><strong>Constraint or verification point</strong>'+conflicts+'</div>':'')+(gaps?'<details class="decision-gaps"><summary>What is not confirmed as a match</summary>'+gaps+'</details>':'')+(r.tradeoff?'<div class="decision-tradeoff"><strong>Important trade-off</strong><span>'+esc(r.tradeoff)+'</span></div>':'')+'<div class="decision-price"><strong class="price">'+(price||'Check current retailer')+'</strong><span class="price-note">'+(price?'maintained price basis — verify current price':'live price not maintained')+'</span></div><div class="actions"><a class="button" href="'+esc(r.url)+'">Inspect decision guide</a><button class="compare-button" type="button" data-compare-product="'+esc(r.slug)+'" aria-pressed="false">Compare</button><button class="save-button" type="button" data-save-product="'+esc(r.slug)+'" aria-pressed="false" aria-label="Save '+esc(r.name)+'">♡</button></div></div></div></article>'}
function renderOutcome(payload,target,form,push){if(!payload||payload.version!=='decision-engine-v4'||payload.commercialRecommendationWeight!==0||!Array.isArray(payload.results))throw new Error('invalid_decision_payload');const main=document.querySelector('main#main');if(!main||!form?.isConnected)throw new Error('missing_live_decision_shell');removeOldOutcome(main,form);resetBusy(form);setStatus(form,'');const results=payload.results,eligible=results.some(r=>r.hardConstraintStatus==='eligible'),noResults=results.length===0;const category=payload.interpretation?.category;const budget=payload.interpretation?.budget;const signals=[category,budget?'Maximum A$'+Number(budget).toLocaleString('en-AU'):'',...(payload.interpretation?.priorities||[]).map(x=>String(x).replace(/-/g,' '))].filter(Boolean).slice(0,10);const wrap=document.createElement('div');wrap.dataset.decisionV50Outcome='';wrap.innerHTML='<section class="section soft-section full-bleed"><div class="wrap"><div class="decision-summary"><div><p class="kicker">What APG understood</p><h2>Your structured decision profile</h2><p>Hard constraints are kept separate from preferences. Missing proof stays visible rather than being guessed.</p></div><div class="decision-signals">'+(signals.length?signals.map(x=>'<span class="pill good">'+esc(x)+'</span>').join(''):'<span class="pill">No specific priority signals detected</span>')+'</div></div></div></section><section class="section"><div class="wrap">'+(payload.audit?.hardConstraintFallback?'<div class="v4-alert"><strong>No clean verified hard-constraint match.</strong> APG is showing maintained alternatives and their conflicts rather than silently breaking your requirements.</div>':'')+'<div class="section-head"><div><p class="kicker">Explainable shortlist</p><h2>'+(noResults?'No maintained candidate yet':eligible?'Best maintained fits':'Closest maintained options')+'</h2><p>Ordered by fit to your decision state. Affiliate availability and commission contribute zero recommendation points.</p></div></div><div class="decision-results">'+(noResults?'<div class="zero-state"><h2>No maintained candidate yet</h2><p>APG could not confidently match this request to a maintained product. Try choosing a maintained category, broadening the description or relaxing one requirement.</p></div>':results.map((r,i)=>resultCard(r,i,eligible)).join(''))+'</div>'+(payload.recommendation?'<div class="v4-panel"><p class="kicker">Why the answer leads here</p><div class="v4-explain"><article><h3>Why the leading option won</h3>'+list(payload.recommendation.whyItWon||[])+'</article><article><h3>When the answer could change</h3>'+list(payload.recommendation.whenTheAnswerWouldChange||[])+'</article></div></div>':'')+'<div class="decision-disclaimer"><strong>Interpretation boundary</strong><p>Decision Lab uses maintained APG evidence. A fit is not a hands-on test score, and unverified requirements are disclosed rather than guessed.</p></div></div></section><section class="section"><div class="wrap"><div id="decisionHistory" hidden></div></div></section>';main.appendChild(wrap);const relative=target.pathname+target.search;if(push&&relative!==(location.pathname+location.search))history.pushState({apgDecisionV50:true},'',relative);else if(!push&&relative!==(location.pathname+location.search))history.replaceState(history.state,'',relative);document.body.setAttribute('data-decision-query',target.searchParams.get('q')||'');document.body.setAttribute('data-decision-url',relative);const share=document.querySelector('[data-share-surface]');if(share)share.dataset.shareUrl=relative;setDecisionState(noResults?'no-results':'success');syncLocalActions();document.dispatchEvent(new CustomEvent('apg:decision-rendered',{detail:{url:relative,patch:'${PATCH}',state:noResults?'no-results':'success'}}));sendEvent(noResults?'decision_lab_no_results':'decision_lab_success',form)}
async function fetchOutcome(target,{push=true,sourceForm=null}={}){const serial=++requestSerial;if(activeController)activeController.abort();const controller=new AbortController();activeController=controller;let deadlineExpired=false;clearTimers();deadlineTimer=setTimeout(()=>{deadlineExpired=true;controller.abort()},DEADLINE_MS);if(sourceForm)slowTimer=setTimeout(()=>{if(serial===requestSerial&&sourceForm.isConnected)setStatus(sourceForm,'This is taking longer than usual. We will stop automatically if it cannot complete.')},SLOW_MS);try{const response=await fetch(target.href,{method:'GET',credentials:'same-origin',cache:'no-store',headers:{Accept:'application/json','X-APG-Decision-JSON':'1'},signal:controller.signal});if(!response.ok)throw new Error('decision_http_'+response.status);const payload=await response.json();if(serial!==requestSerial)return false;renderOutcome(payload,target,sourceForm||document.querySelector('form.decision-form'),push);return true}catch(error){if(deadlineExpired){const e=new Error('decision_deadline_exceeded');e.name='APGDecisionTimeout';throw e}throw error}finally{if(serial===requestSerial){clearTimers();activeController=null}}}
async function submitDecision(event,form){event.preventDefault();event.stopImmediatePropagation();if(form.dataset.apgDecisionV50Submitting==='true'){setStatus(form,'Your shortlist is already being built.');return}const target=safeTarget(form);if(!hasInput(target)){setDecisionState('validation');setStatus(form,'Describe what you want or choose at least one filter so Decision Lab has something to match.',true);resetBusy(form);return}const q=String(target.searchParams.get('q')||'');if(q.length>2000){setDecisionState('validation');setStatus(form,'That description is too long for a reliable shareable decision URL. Please shorten it to under 2,000 characters and try again.',true);resetBusy(form);return}setBusy(form);sendEvent(form.dataset.apgDecisionV50TimedOut==='true'?'decision_lab_retry':'decision_lab_submitted',form);delete form.dataset.apgDecisionV50TimedOut;try{await fetchOutcome(target,{push:true,sourceForm:form})}catch(error){if(!form.isConnected)return;resetBusy(form);const timedOut=error?.name==='APGDecisionTimeout'||error?.name==='AbortError'||String(error?.message||'').includes('aborted');if(timedOut){setDecisionState('timeout');form.dataset.apgDecisionV50TimedOut='true';setStatus(form,'Decision Lab could not complete that request in time. Your description and filters are still here — please try again.',true);sendEvent('decision_lab_timeout',form)}else{setDecisionState('error');setStatus(form,'Decision Lab could not complete that request just now. Your description and filters are still here — please try again.',true);sendEvent('decision_lab_error',form)}}}
function captureSubmit(event){const form=event.target instanceof HTMLFormElement?event.target:null;if(isDecisionForm(form))submitDecision(event,form)}
window.addEventListener('submit',captureSubmit,true);
window.addEventListener('pagehide',()=>{if(activeController)activeController.abort();clearTimers()},{capture:true});
window.addEventListener('popstate',()=>{if(location.pathname!=='/decision-lab/')return;const target=new URL(location.href),form=document.querySelector('form.decision-form');if(!hasInput(target)){location.reload();return}syncFormFromTarget(form,target);setBusy(form);fetchOutcome(target,{push:false,sourceForm:form}).catch(()=>location.reload())});
document.addEventListener('click',event=>{const copy=event.target instanceof Element?event.target.closest('[data-copy-decision]'):null;if(!copy)return;const status=document.querySelector('[data-copy-status]'),value=location.href;if(navigator.clipboard?.writeText)navigator.clipboard.writeText(value).then(()=>{if(status)status.textContent='Decision link copied'}).catch(()=>{if(status)status.textContent='Copy the page address from your browser'})});
setDecisionState(document.querySelector('.decision-result')?'success':document.querySelector('.zero-state')?'no-results':'idle');syncLocalActions();
})();
`;

function sendAsset(req,res){res.statusCode=200;res.setHeader('Content-Type','application/javascript; charset=utf-8');res.setHeader('Cache-Control','public, max-age=0, must-revalidate');res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('X-APG-Decision-Lab-Resilience',PATCH);return res.end(req.method==='HEAD'?'':clientJs)}
function sendDecisionJson(req,res,url){const q=url.searchParams.get('q')||'',opts={category:url.searchParams.get('category')||'',budget:url.searchParams.get('budget')||'',brand:url.searchParams.get('brand')||''};try{const payload=publicDecision(q,opts);res.statusCode=200;res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','no-store');res.setHeader('X-APG-Decision-Lab-Resilience',PATCH);return res.end(req.method==='HEAD'?'':JSON.stringify(payload))}catch(error){console.error('decision_lab_json_error',{name:error?.name||'Error',message:String(error?.message||error).slice(0,300)});res.statusCode=500;res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','no-store');return res.end(JSON.stringify({error:'decision_unavailable',version:ENGINE}))}}
function inject(html){let out=String(html||'');if(out.includes(ASSET_PATH))return out;const tag=`<script src="${ASSET_PATH}?v=${VERSION}" defer></script>`,appMarker='<script src="/assets/app.js';if(out.includes(appMarker))return out.replace(appMarker,tag+appMarker);if(out.includes('</head>'))return out.replace('</head>',tag+'</head>');return out}
function handler(req,res){let url;try{url=new URL(req.url,'https://australianproductguide.au')}catch{url=new URL('https://australianproductguide.au/')}if(url.pathname===ASSET_PATH)return sendAsset(req,res);if(url.pathname==='/decision-lab/'&&String(req.headers?.[JSON_HEADER]||'')==='1')return sendDecisionJson(req,res,url);if(url.pathname==='/decision-lab/')res.setHeader('X-APG-Decision-Lab-Resilience',PATCH);const end=res.end.bind(res);res.end=(body,...args)=>{const type=String(res.getHeader('Content-Type')||'').toLowerCase();if(req.method!=='HEAD'&&res.statusCode===200&&typeof body==='string'&&type.startsWith('text/html')&&url.pathname==='/decision-lab/'){const next=inject(body);if(next!==body){body=next;res.removeHeader('Content-Length')}}return end(body,...args)};return downstream(req,res)}
Object.assign(handler,downstream,{ASSET_PATH,VERSION,PATCH,ENGINE,JSON_HEADER,clientJs,inject,sendDecisionJson});
module.exports=handler;

const {categories,pathways,products}=require('../data');
const {trust,brands,pairPages,indexableRoutes,slugify}=require('./routes');
const trustContent=require('./content');
const {searchSite}=require('./search');
const {esc,origin,layout,hero,productCard,categoryCard,retailerPanel,sourceBlock,productVisual,pill,priceContext,globalSearch}=require('./ui');
const homeCrumb={name:'Home',path:'/'};
const human=x=>String(x||'').replace(/-/g,' ').replace(/\b\w/g,m=>m.toUpperCase());
const money=n=>n?`A$${Number(n).toLocaleString('en-AU')}`:'Check current retailer';
function overlap(a,b){return (a.tags||[]).filter(x=>(b.tags||[]).includes(x)).length;}
function alternativesFor(p,n=3){return products.filter(x=>x.category===p.category&&x.slug!==p.slug).sort((a,b)=>overlap(p,b)-overlap(p,a)).slice(0,n);}
function comparisonLinks(p,n=3){return pairPages.filter(x=>x.a.slug===p.slug||x.b.slug===p.slug).slice(0,n);}
function categoryPicks(c){
  const value=c.products.find(p=>p.tags.includes('value'))||c.products[0];
  const premiumTagged=c.products.find(p=>p.tags.includes('premium'));const premium=premiumTagged||[...c.products].filter(p=>p.price).sort((a,b)=>(b.price||0)-(a.price||0))[0]||c.products[c.products.length-1];
  const balanced=[...c.products].sort((a,b)=>((new Set(b.tags)).size+(b.highlights||[]).length)-((new Set(a.tags)).size+(a.highlights||[]).length))[0];
  const specialist=c.products.find(p=>p.tags.includes(c.priorities[0])&&p.slug!==balanced.slug)||c.products[1]||c.products[0];
  return [
    {label:'Balanced starting point',p:balanced,why:'A broad fit across several maintained decision signals.'},
    {label:'Value starting point',p:value,why:'Carries the maintained value signal; price still needs a current retailer check.'},
    {label:premiumTagged?'Premium starting point':'Feature-rich starting point',p:premium,why:premiumTagged?'A premium-positioned option for shoppers prioritising higher-tier features.':'A broader-feature option where the maintained data does not support a formal premium label.'},
    {label:`For ${human(c.priorities[0]).toLowerCase()}`,p:specialist,why:`A useful starting point when ${human(c.priorities[0]).toLowerCase()} matters most.`}
  ];
}
function pickCards(c){return categoryPicks(c).map(x=>`<article class="feature-card"><p class="eyebrow">${esc(x.label)}</p><h3><a href="/products/${x.p.slug}/">${esc(x.p.name)}</a></h3><p>${esc(x.why)}</p><div class="pills">${x.p.tags.slice(0,3).map(t=>pill(human(t))).join('')}</div><a class="text-link" href="/products/${x.p.slug}/">See why it fits →</a></article>`).join('');}
function faqBlock(c){return `<section class="section"><div class="section-head"><div><p class="kicker">Common questions</p><h2>${esc(c.label)} buying questions</h2></div></div><div class="faq">${c.faqs.map(x=>`<details><summary>${esc(x[0])}</summary><p>${esc(x[1])}</p></details>`).join('')}</div></section>`;}
function home(req){
  const featured=Object.values(categories).map(c=>pairPages.find(x=>x.category===c.slug)).filter(Boolean);
  const newest=products.slice(0,6);
  const shortcuts=[['Coffee machine under A$800','best coffee machine under $800','Budget-aware coffee shortlist'],['Robot vacuum for pet hair','robot vacuum for pet hair','Pet-focused robot vacuum options'],['Air fryer for a family','air fryer for family of 4','Family-capacity air fryer options'],['Best-value headphones','best value headphones','Value-oriented wireless headphones']];
  const body=`<section class="home-hero"><div class="wrap home-hero-grid"><div><p class="kicker">Independent Australian product discovery</p><h1>Compare smarter.<br>Buy with confidence.</h1><p class="lede">Tell us what matters, compare the real differences, inspect the evidence and move to the right retailer without repeating your research.</p>${globalSearch()}<div class="hero-links"><a href="/categories/">Browse categories</a><a href="/methodology/">How recommendations work</a><a href="/coverage/">See current coverage</a></div></div><div class="decision-preview"><p class="eyebrow">APG decision model</p><h2>From “what should I buy?” to a reasoned shortlist.</h2><ol><li><strong>Describe the need</strong><span>Budget, use case, priorities and deal-breakers.</span></li><li><strong>See the differences</strong><span>Products translated into suitability, not spec-sheet noise.</span></li><li><strong>Inspect why</strong><span>Evidence sources, trade-offs and commercial independence.</span></li><li><strong>Choose where to buy</strong><span>Verified direct retailer links where possible; safe fallbacks otherwise.</span></li></ol></div></div></section>
  <section class="trust-strip wrap"><div><strong>37</strong><span>maintained products</span></div><div><strong>4</strong><span>live comparison categories</span></div><div><strong>48</strong><span>category pathways</span></div><div><strong>0</strong><span>commercial points in suitability</span></div></section>
  <section class="section"><div class="wrap"><div class="section-head"><div><p class="kicker">Browse by category</p><h2>Start with the decision you are making</h2><p>Four categories currently have maintained product evidence, comparisons and recommendation journeys.</p></div><a class="text-link" href="/categories/">View all pathways →</a></div><div class="category-grid">${Object.values(categories).map(categoryCard).join('')}</div></div></section>
  <section class="section soft-section"><div class="wrap"><div class="section-head"><div><p class="kicker">Help Me Choose</p><h2>Tell APG what matters to you</h2><p>Recommendation journeys narrow the market using stated priorities and transparent rules—not commission.</p></div></div><div class="grid four">${Object.values(categories).map(c=>`<article class="feature-card"><p class="eyebrow">${esc(c.label)}</p><h3>Find a better-fit ${esc(c.label.toLowerCase().replace(/s$/,''))}</h3><p>${esc(c.factors.slice(0,2).join('. '))}.</p><a class="button secondary" href="/categories/${c.slug}/finder/">Start finder</a></article>`).join('')}</div></div></section>
  <section class="section"><div class="wrap"><div class="section-head"><div><p class="kicker">Decision shortcuts</p><h2>Search the way you actually think</h2><p>APG interprets categories, brands, use cases, model names and price ceilings.</p></div></div><div class="grid four">${shortcuts.map(([title,q,copy])=>`<a class="query-card" href="/search/?q=${encodeURIComponent(q)}"><span class="query-icon">↗</span><strong>${esc(title)}</strong><span>${esc(copy)}</span></a>`).join('')}</div></div></section>
  <section class="section soft-section"><div class="wrap"><div class="section-head"><div><p class="kicker">Featured head-to-heads</p><h2>Skip to the differences that decide the purchase</h2></div></div><div class="grid two">${featured.map(x=>`<article class="comparison-card"><p class="eyebrow">${esc(categories[x.category].label)}</p><h3><a href="${x.path}">${esc(x.a.name)} <span>vs</span> ${esc(x.b.name)}</a></h3><p>Compare suitability, strengths, compromises and retailer pathways side by side.</p><a class="text-link" href="${x.path}">Open comparison →</a></article>`).join('')}</div></div></section>
  <section class="section"><div class="wrap"><div class="section-head"><div><p class="kicker">Recently reviewed catalogue</p><h2>Maintained product decision guides</h2><p>Every maintained product states its evidence status, source and review date.</p></div><a href="/updates/" class="text-link">Current update status →</a></div><div class="grid">${newest.map(productCard).join('')}</div><div id="recentlyViewed" hidden></div></div></section>
  <section class="section trust-section"><div class="wrap decision-layout"><div><p class="kicker">Why trust APG?</p><h2>Transparency is part of the product.</h2><p>APG does not claim a testing lab it does not have, invent reviews or let affiliate economics change product fit. The objective is to make the reasoning inspectable.</p><div class="actions"><a class="button" href="/methodology/">Read the methodology</a><a class="button secondary" href="/editorial-standards/">Editorial standards</a></div></div><div class="grid two"><div class="feature-card"><strong>Primary-source evidence</strong><p>Consequential claims are anchored to manufacturer information.</p></div><div class="feature-card"><strong>Testing status disclosed</strong><p>Current guidance is labelled desk-researched/specification-based unless stated otherwise.</p></div><div class="feature-card"><strong>Corrections propagate</strong><p>Material changes should flow through product pages, comparisons and finder logic.</p></div><div class="feature-card"><strong>Retailer links separated</strong><p>Where-to-buy options sit after the decision reasoning, not inside the score.</p></div></div></div></section>`;
  return layout(req,{title:'Australian Product Guide | Compare Smarter. Buy with Confidence.',description:'Australian product discovery with personalised suitability, transparent comparisons, evidence and retailer pathways.',path:'/',body});
}
function categoriesIndex(req){const body=hero('Product discovery','Browse product categories','Start with a maintained decision hub or explore what Australian Product Guide is researching next.')+`<section class="section"><div class="section-head"><div><p class="kicker">Live now</p><h2>Maintained comparison categories</h2><p>These categories have structured product records, recommendation journeys and active comparison QA.</p></div></div><div class="category-grid">${Object.values(categories).map(categoryCard).join('')}</div></section><section class="section soft-panel"><div class="section-head"><div><p class="kicker">Research roadmap</p><h2>48 category pathways</h2><p>Research-queue categories remain noindex until the evidence and maintenance workflow are ready.</p></div><a href="/coverage/" class="text-link">Coverage policy →</a></div><ul class="pathway-list">${pathways.map(p=>`<li><a href="/categories/${p.slug}/">${esc(p.label)}</a> ${p.maintained?pill('Maintained','good'):pill('Research queue')}</li>`).join('')}</ul></section>`;return layout(req,{title:'Product Categories Australia | Australian Product Guide',description:'Browse maintained Australian product comparison hubs and APG’s transparent category research roadmap.',path:'/categories/',body,crumbs:[homeCrumb,{name:'Categories',path:'/categories/'}]});}
function categoryPage(req,c,u){const path=`/categories/${c.slug}/`,priority=(u.searchParams.get('priority')||'').toLowerCase(),brand=(u.searchParams.get('brand')||'').toLowerCase(),sort=u.searchParams.get('sort')||'recommended';let list=[...c.products];if(priority)list=list.filter(p=>p.tags.includes(priority));if(brand)list=list.filter(p=>slugify(p.brand)===brand);if(sort==='name')list.sort((a,b)=>a.name.localeCompare(b.name));else if(sort==='price')list.sort((a,b)=>(a.price||999999)-(b.price||999999));else list.sort((a,b)=>b.tags.length-a.tags.length||a.name.localeCompare(b.name));const brandsIn=[...new Set(c.products.map(p=>p.brand))].sort(),categoryPairs=pairPages.filter(x=>x.category===c.slug).slice(0,6);const body=hero('Australian buying hub',c.title,c.description,`<a class="button" href="${path}finder/">Help me choose</a><a class="button secondary" href="/compare/${c.slug}/">Compare products</a>`)+`<section class="section"><div class="section-head"><div><p class="kicker">Decision shortcuts</p><h2>Useful starting points—not universal winners</h2><p>These labels are transparent editorial entry points into the maintained set. Your best fit can differ.</p></div></div><div class="grid four">${pickCards(c)}</div></section><section class="section"><div class="section-head"><div><p class="kicker">Product catalogue</p><h2>Explore ${c.products.length} maintained products</h2><p>Filter by the use-case signals in the current dataset, then add 2–4 products to the comparison tray.</p></div><span class="independence-badge">Reviewed 15 Aug 2026</span></div><form class="filter-bar" method="get"><div><label for="priority">Priority</label><select id="priority" name="priority"><option value="">All priorities</option>${c.priorities.map(x=>`<option value="${x}"${x===priority?' selected':''}>${esc(human(x))}</option>`).join('')}</select></div><div><label for="brand">Brand</label><select id="brand" name="brand"><option value="">All brands</option>${brandsIn.map(b=>`<option value="${slugify(b)}"${slugify(b)===brand?' selected':''}>${esc(b)}</option>`).join('')}</select></div><div><label for="sort">Sort</label><select id="sort" name="sort"><option value="recommended"${sort==='recommended'?' selected':''}>Decision relevance</option><option value="name"${sort==='name'?' selected':''}>Product name</option>${c.products.some(p=>p.price)?`<option value="price"${sort==='price'?' selected':''}>Maintained price basis</option>`:''}</select></div><button class="button compact" type="submit">Apply</button></form>${list.length?`<div class="grid">${list.map(productCard).join('')}</div>`:`<div class="zero-state"><h3>No products match those filters</h3><p>Clear one filter or use Help Me Choose to explore the full maintained set.</p><a class="button secondary" href="${path}">Clear filters</a></div>`}</section><section class="section soft-section full-bleed"><div class="wrap"><div class="section-head"><div><p class="kicker">What matters</p><h2>Compare the decision factors before the feature list</h2></div><a href="/guides/${c.slug}-buying-guide/" class="text-link">Read buying guide →</a></div><div class="grid four">${c.factors.map((f,i)=>`<article class="feature-card"><span class="step-number">0${i+1}</span><h3>${esc(f)}</h3><p>Use this factor to rule products in or out before chasing secondary features.</p></article>`).join('')}</div></div></section><section class="section"><div class="section-head"><div><p class="kicker">Head-to-head format</p><h2>Featured comparisons</h2><p>These are featured editorial comparisons, not traffic-popularity claims.</p></div></div><div class="grid two">${categoryPairs.map(x=>`<article class="comparison-card"><h3><a href="${x.path}">${esc(x.a.name)} <span>vs</span> ${esc(x.b.name)}</a></h3><p>${esc(x.a.brand)} and ${esc(x.b.brand)} compared through documented strengths, trade-offs and use cases.</p><a href="${x.path}" class="text-link">Compare them →</a></article>`).join('')}</div></section>${faqBlock(c)}<aside class="evidence-box"><p class="kicker">Category evidence</p><h2>Maintained, dated and inspectable</h2><p>This category contains ${c.products.length} maintained product records reviewed against primary manufacturer evidence. Product pages expose their source and testing status individually. Commercial relationships contribute zero recommendation points.</p><div class="actions"><a class="button secondary" href="/methodology/">Methodology</a><a class="button secondary" href="/sources/">Source policy</a></div></aside>`;return layout(req,{title:`${c.title} | Compare ${c.products.length} Products`,description:c.description,path,body,crumbs:[homeCrumb,{name:'Categories',path:'/categories/'},{name:c.label,path}]});}
function pathwayPage(req,p){const path=`/categories/${p.slug}/`;const body=hero('Research queue',`${p.label} buying pathway`,`APG is researching this category, but it does not yet publish a maintained ranking or finder.`)+`<section class="section"><div class="zero-state"><p class="eyebrow">Not yet maintained</p><h2>No manufactured “best” list here.</h2><p>Before this category goes live, APG will define the buyer decision factors, verify Australian model coverage, create a structured evidence set and establish an update workflow.</p><div class="actions"><a class="button" href="/categories/">Browse live categories</a><a class="button secondary" href="/coverage/">How coverage works</a></div></div></section>`;return layout(req,{title:`${p.label} | Research Queue | Australian Product Guide`,description:`${p.label} is an APG research pathway awaiting a maintained Australian evidence set.`,path,body,noindex:true,crumbs:[homeCrumb,{name:'Categories',path:'/categories/'},{name:p.label,path}]});}
function specRows(p){const rows=[['Brand',p.brand],['Category',p.categoryLabel],['Testing status',p.testingStatus],['Last reviewed','15 August 2026']];if(p.category==='coffee-machines')rows.push(['Workflow',human(p.workflow)],['Milk system',human(p.milk)],['Cold-drink support',p.cold?'Documented':'Not documented in maintained data'],['Dimensions',`${p.width} mm W × ${p.depth} mm D`],['Bean switching',p.beanSwitch?'Designed to support easier switching':'No dedicated switch system in maintained data'],['Maintained price basis',money(p.price)]);else rows.push(['Decision signals',p.tags.map(human).join(', ')],['Key documented points',p.highlights.join('; ')]);return rows;}
function productPage(req,p){const c=categories[p.category],path=`/products/${p.slug}/`,alts=alternativesFor(p),comparisons=comparisonLinks(p),bestFor=p.tags.slice(0,4).map(human),schema={'@context':'https://schema.org','@type':'Product',name:p.name,brand:{'@type':'Brand',name:p.brand},description:p.summary,url:origin(req)+path},direct=p.retailers.some(r=>r.kind==='affiliate-direct');const body=`<section class="product-hero"><div class="wrap product-hero-grid">${productVisual(p,true)}<div><div class="product-meta"><span>${esc(p.categoryLabel)}</span><span>Reviewed 15 Aug 2026</span></div><p class="kicker">${esc(p.brand)}</p><h1>${esc(p.name)}</h1><p class="lede">${esc(p.summary)}</p><div class="pills">${bestFor.map(x=>pill(`Fit signal: ${x}`,'good')).join('')}</div><div class="product-price">${priceContext(p)}</div><div class="actions"><a class="button" href="#where-to-buy">Where to buy</a><button class="compare-button" type="button" data-compare-product="${p.slug}">Add to compare</button></div><p class="fine-inline">${direct?'Exact Amazon Australia individual listing verified for this product.':'Amazon journey uses a model-specific search fallback because an exact individual listing has not been independently verified.'}</p></div></div></section><section class="section"><div class="wrap decision-layout"><div><p class="kicker">Suitability snapshot</p><h2>Who this product is most likely to suit</h2><p>${esc(p.summary)}</p><div class="pills">${bestFor.map(x=>pill(x,'good')).join('')}</div><h3>Why it is in the maintained set</h3><ul>${p.highlights.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div><aside class="decision-card"><p class="eyebrow">The important compromise</p><h3>Know this before you buy</h3><p>${esc(p.watch)}</p><a href="/categories/${c.slug}/finder/" class="text-link">Check it against your priorities →</a></aside></div></section><section class="section soft-section full-bleed"><div class="wrap"><div class="section-head"><div><p class="kicker">Key information</p><h2>The decision facts at a glance</h2></div></div><dl class="facts-grid">${specRows(p).map(([k,v])=>`<div class="fact-card"><dt>${esc(k)}</dt><dd>${esc(v)}</dd></div>`).join('')}</dl></div></section><section class="section"><div class="wrap pros-cons"><div class="pros"><p class="eyebrow">Reasons to shortlist</p><h2>Strengths</h2><ul>${p.highlights.map(x=>`<li>${esc(x)}</li>`).join('')}</ul><p>These are documented product characteristics, not a claim of hands-on performance testing.</p></div><div class="cons"><p class="eyebrow">Reasons to look elsewhere</p><h2>Limitations</h2><p>${esc(p.watch)}</p><p>If that compromise conflicts with your priorities, compare the alternatives below rather than forcing a fit.</p></div></div></section><section class="section"><div class="wrap"><div class="section-head"><div><p class="kicker">Closest alternatives</p><h2>Other products worth comparing</h2><p>Selected by category and overlapping suitability signals—not by affiliate economics.</p></div></div><div class="grid">${alts.map(productCard).join('')}</div>${comparisons.length?`<h3>Open a head-to-head</h3><div class="link-list">${comparisons.map(x=>`<a href="${x.path}">${esc(x.a.name)} vs ${esc(x.b.name)} →</a>`).join('')}</div>`:''}</div></section><div id="where-to-buy" class="wrap">${retailerPanel(p)}${sourceBlock(p)}</div>`;return layout(req,{title:`${p.name} Australia | Decision Guide & Comparison`,description:`Who ${p.name} suits, key trade-offs, alternatives, evidence and Australian retailer pathways.`,path,body,schemas:[schema],crumbs:[homeCrumb,{name:c.label,path:`/categories/${c.slug}/`},{name:p.name,path}],bodyData:`data-product-slug="${esc(p.slug)}"`});}
function guidePage(req,c){const path=`/guides/${c.slug}-buying-guide/`;const body=hero('Australian buying guide',`${c.label}: what actually matters before you buy`,`Use the decision factors below to narrow the market before comparing secondary features or retailer promotions.`,`<a class="button" href="/categories/${c.slug}/finder/">Help me choose</a><a class="button secondary" href="/compare/${c.slug}/">Compare maintained products</a>`)+`<section class="section guide-layout"><div><h2>Start with four decisions</h2>${c.factors.map((x,i)=>`<article class="guide-step"><span class="step-number">0${i+1}</span><div><h3>${esc(x)}</h3><p>Decide the constraint or preference here first. A feature is only valuable if it improves your use case.</p></div></article>`).join('')}<h2>Why APG avoids one universal winner</h2><p>The maintained ${esc(c.label.toLowerCase())} set contains ${c.products.length} products selected to represent meaningful differences. A product that is excellent for one household can be a poor fit for another because budget, workflow, space or priorities differ.</p><h2>Use a three-step shortlist</h2><ol><li>Use <a href="/categories/${c.slug}/finder/">Help Me Choose</a>.</li><li>Use the <a href="/compare/${c.slug}/">comparison hub</a> or select 2–4 products.</li><li>Open product guides to inspect evidence and retailer options.</li></ol>${faqBlock(c)}</div><aside class="guide-aside"><div class="feature-card"><p class="eyebrow">Current coverage</p><h3>${c.products.length} maintained products</h3><p>Reviewed against primary manufacturer evidence.</p><a href="/coverage/">Coverage policy →</a></div><div class="feature-card"><p class="eyebrow">Commercial independence</p><h3>0 recommendation points</h3><p>Retailer status does not improve suitability.</p><a href="/affiliate-disclosure/">Affiliate disclosure →</a></div></aside></section>`;const schema={'@context':'https://schema.org','@type':'Article',headline:`${c.label}: what matters before you buy`,dateModified:'2026-08-16',author:{'@type':'Organization',name:'Australian Product Guide'}};return layout(req,{title:`${c.label} Buying Guide Australia | Australian Product Guide`,description:`A practical Australian ${c.label.toLowerCase()} buying guide focused on the decision factors that change suitability.`,path,body,schemas:[schema],crumbs:[homeCrumb,{name:'Categories',path:'/categories/'},{name:c.label,path:`/categories/${c.slug}/`},{name:'Buying guide',path}]});}
function genericFinderScore(p,q){let s=50,reasons=[];if(q.priority&&p.tags.includes(q.priority)){s+=28;reasons.push(`Matches your primary priority: ${human(q.priority)}`);}if(q.secondary&&p.tags.includes(q.secondary)){s+=14;reasons.push(`Also matches ${human(q.secondary)}`);}if(q.position==='value'&&p.tags.includes('value')){s+=12;reasons.push('Carries the maintained value signal');}if(q.position==='premium'&&p.tags.includes('premium')){s+=12;reasons.push('Fits a premium-feature preference');}if(q.brand&&slugify(p.brand)===q.brand){s+=10;reasons.push(`Matches your ${p.brand} brand preference`);}return {p,score:s,reasons};}
function finderPage(req,c,u){const path=`/categories/${c.slug}/finder/`,q={priority:u.searchParams.get('priority')||'',secondary:u.searchParams.get('secondary')||'',position:u.searchParams.get('position')||'',brand:u.searchParams.get('brand')||''},complete=!!q.priority,ranked=c.products.map(p=>genericFinderScore(p,q)).sort((a,b)=>b.score-a.score||a.p.name.localeCompare(b.p.name));let result='';if(complete){const best=ranked[0],value=ranked.find(x=>x.p.tags.includes('value')&&x.p.slug!==best.p.slug),premium=ranked.find(x=>x.p.tags.includes('premium')&&x.p.slug!==best.p.slug),other=ranked.find(x=>![best.p.slug,value?.p.slug,premium?.p.slug].includes(x.p.slug)),alt=[value&&{label:'Best-value alternative',x:value},premium&&{label:'Premium alternative',x:premium},other&&{label:'Another option',x:other}].filter(Boolean);result=`<div class="result-hero"><p class="eyebrow">Best match for your answers</p><div class="result-grid">${productVisual(best.p)}<div><h2><a href="/products/${best.p.slug}/">${esc(best.p.name)}</a></h2><p>${esc(best.p.summary)}</p><div class="reason-list">${(best.reasons.length?best.reasons:['Best overall match to the stated filters in the maintained dataset']).map(x=>`<div class="reason">${esc(x)}</div>`).join('')}</div><p class="fine-inline">Fit score is a transparent shortlist signal, not a performance rating.</p><div class="actions"><a class="button" href="/products/${best.p.slug}/">View decision guide</a><button class="compare-button" type="button" data-compare-product="${best.p.slug}">Add to compare</button></div></div></div></div>${alt.length?`<h2>Useful alternatives</h2><div class="alternatives-grid">${alt.map(({label,x})=>`<article class="alternative-card"><p class="eyebrow">${label}</p><h3><a href="/products/${x.p.slug}/">${esc(x.p.name)}</a></h3><p>${esc(x.p.watch)}</p></article>`).join('')}</div>`:''}`;}const brandsIn=[...new Set(c.products.map(p=>p.brand))].sort();const body=hero('Personalised product discovery',`Help Me Choose ${c.label}`,`Tell APG what matters. You will get a best match plus useful alternatives and the reasons behind each recommendation.`)+`<section class="section finder-shell"><form class="finder" method="get" data-busy-form><div class="finder-field"><label for="priority">1. What matters most?</label><select id="priority" name="priority" required><option value="">Choose your main priority</option>${c.priorities.map(x=>`<option value="${x}"${x===q.priority?' selected':''}>${esc(human(x))}</option>`).join('')}</select></div><div class="finder-field"><label for="secondary">2. What else matters?</label><select id="secondary" name="secondary"><option value="">No second priority</option>${c.priorities.map(x=>`<option value="${x}"${x===q.secondary?' selected':''}>${esc(human(x))}</option>`).join('')}</select></div><div class="finder-field"><label for="position">3. Market position</label><select id="position" name="position"><option value="balanced">Balanced</option><option value="value"${q.position==='value'?' selected':''}>Value-conscious</option><option value="premium"${q.position==='premium'?' selected':''}>Premium features</option></select></div><div class="finder-field"><label for="brand">4. Brand preference</label><select id="brand" name="brand"><option value="">Open to any brand</option>${brandsIn.map(b=>`<option value="${slugify(b)}"${slugify(b)===q.brand?' selected':''}>${esc(b)}</option>`).join('')}</select></div><button class="button" type="submit">Find my matches</button><p class="fine-inline">No account required. Commercial relationships contribute zero points.</p></form><div>${result||`<div class="finder-empty"><p class="eyebrow">What you will get</p><h2>A reasoned shortlist, not a mystery score</h2><ul><li>Best match and why it fits</li><li>Value and premium alternatives where supported</li><li>Important compromises</li><li>Evidence and comparison links</li></ul></div>`}</div></section>`;return layout(req,{title:`Help Me Choose ${c.label} | Australian Product Guide`,description:`A transparent ${c.label.toLowerCase()} finder that explains why products fit your stated priorities.`,path,body,crumbs:[homeCrumb,{name:c.label,path:`/categories/${c.slug}/`},{name:'Help Me Choose',path}]});}
function compareIndex(req,c){const path=`/compare/${c.slug}/`,rows=c.products.map(p=>`<tr><td><a href="/products/${p.slug}/"><strong>${esc(p.name)}</strong></a><br><span class="muted">${esc(p.brand)}</span><br><button class="compare-button" type="button" data-compare-product="${p.slug}">Add to compare</button></td><td>${p.tags.slice(0,4).map(t=>pill(human(t))).join('')}</td><td>${p.highlights.map(esc).join('<br>')}</td><td>${esc(p.watch)}</td><td>${priceContext(p)}</td></tr>`).join(''),pairs=pairPages.filter(x=>x.category===c.slug).slice(0,8);const body=hero('Decision comparison',`Compare ${c.label.toLowerCase()}`,`Scan the maintained set, add 2–4 products to your comparison tray, or jump into a prepared head-to-head.`)+`<section class="section"><div class="notice"><strong>Make it personal:</strong> use “Add to compare” on any 2–4 products. Your selection is remembered on this device.</div><div class="compare-wrap"><table class="compare"><thead><tr><th>Product</th><th>Best-fit signals</th><th>Key points</th><th>Important compromise</th><th>Price context</th></tr></thead><tbody>${rows}</tbody></table></div></section><section class="section"><div class="section-head"><div><p class="kicker">Prepared head-to-heads</p><h2>Start with a focused comparison</h2></div></div><div class="grid two">${pairs.map(x=>`<article class="comparison-card"><h3><a href="${x.path}">${esc(x.a.name)} <span>vs</span> ${esc(x.b.name)}</a></h3><p>Which use cases favour each product, where they compromise and what to verify before purchase.</p></article>`).join('')}</div></section>`;return layout(req,{title:`Compare ${c.label} Australia | Australian Product Guide`,description:`Decision-oriented side-by-side comparison of ${c.products.length} maintained ${c.label.toLowerCase()} for Australian shoppers.`,path,body,crumbs:[homeCrumb,{name:c.label,path:`/categories/${c.slug}/`},{name:'Compare',path}]});}
function customCompare(req,u){const path='/compare/custom/',slugs=(u.searchParams.get('products')||'').split(',').filter(Boolean).slice(0,4),chosen=slugs.map(s=>products.find(p=>p.slug===s)).filter(Boolean);let body=hero('Your comparison','Compare your shortlist','Select 2–4 maintained products from category or product pages. Your browser keeps the current shortlist on this device.');if(chosen.length<2)body+=`<section class="section"><div class="zero-state"><h2>Select at least two products</h2><p>Use “Add to compare” on product cards, then return here from the sticky comparison tray.</p><a class="button" href="/categories/">Browse categories</a></div></section>`;else{const rows=[['Brand',p=>p.brand],['Category',p=>p.categoryLabel],['Best-fit signals',p=>p.tags.slice(0,5).map(human).join(', ')],['Price context',p=>p.price?money(p.price)+' maintained basis':'Check current retailer'],['Key points',p=>p.highlights.join(' · ')],['Important compromise',p=>p.watch],['Evidence status',p=>p.testingStatus]];body+=`<section class="section"><div class="compare-products">${chosen.map(p=>`<div>${productVisual(p)}<h3><a href="/products/${p.slug}/">${esc(p.name)}</a></h3></div>`).join('')}</div><div class="compare-wrap"><table class="compare"><thead><tr><th>Decision point</th>${chosen.map(p=>`<th>${esc(p.name)}</th>`).join('')}</tr></thead><tbody>${rows.map(([k,fn])=>`<tr><td>${esc(k)}</td>${chosen.map(p=>`<td>${esc(fn(p))}</td>`).join('')}</tr>`).join('')}</tbody></table></div><div class="notice"><strong>Which should you buy?</strong> There is no universal winner across mixed categories or priorities. Use the fit signals and compromises above.</div></section>`;}return layout(req,{title:'Your Product Comparison | Australian Product Guide',description:'Compare 2–4 maintained Australian Product Guide products side by side.',path,body,noindex:true,crumbs:[homeCrumb,{name:'Your comparison',path}]});}
function uniqueTags(a,b){return {a:a.tags.filter(x=>!b.tags.includes(x)),b:b.tags.filter(x=>!a.tags.includes(x))};}
function pairPage(req,x){const c=categories[x.category],path=x.path,u=uniqueTags(x.a,x.b),sameTags=x.a.tags.filter(t=>x.b.tags.includes(t)),priceNote=x.a.price&&x.b.price?`${x.a.name}: ${money(x.a.price)} maintained basis; ${x.b.name}: ${money(x.b.price)} maintained basis.`:'Current live retailer pricing is not maintained for both products; check retailer options before purchase.';const body=hero('Head-to-head decision',`${x.a.name} vs ${x.b.name}`,`Which option better fits your priorities? Compare documented strengths, distinctive use cases, compromises and retailer pathways.`)+`<section class="section comparison-summary"><article class="winner-card recommended"><p class="eyebrow">Choose ${esc(x.a.name)} if…</p><h2>${esc(x.a.name)}</h2><div class="pills">${(u.a.length?u.a:x.a.tags.slice(0,3)).map(t=>pill(human(t),'good')).join('')}</div><p>${esc(x.a.summary)}</p><p><strong>Watch:</strong> ${esc(x.a.watch)}</p><a href="/products/${x.a.slug}/" class="button secondary">Open product guide</a></article><article class="winner-card"><p class="eyebrow">Choose ${esc(x.b.name)} if…</p><h2>${esc(x.b.name)}</h2><div class="pills">${(u.b.length?u.b:x.b.tags.slice(0,3)).map(t=>pill(human(t),'good')).join('')}</div><p>${esc(x.b.summary)}</p><p><strong>Watch:</strong> ${esc(x.b.watch)}</p><a href="/products/${x.b.slug}/" class="button secondary">Open product guide</a></article></section><section class="section"><div class="compare-wrap"><table class="compare"><thead><tr><th>Decision point</th><th>${esc(x.a.name)}</th><th>${esc(x.b.name)}</th></tr></thead><tbody><tr><td>Best-fit signals</td><td>${x.a.tags.map(t=>pill(human(t))).join('')}</td><td>${x.b.tags.map(t=>pill(human(t))).join('')}</td></tr><tr><td>Key documented points</td><td>${x.a.highlights.map(esc).join('<br>')}</td><td>${x.b.highlights.map(esc).join('<br>')}</td></tr><tr><td>Important compromise</td><td>${esc(x.a.watch)}</td><td>${esc(x.b.watch)}</td></tr><tr><td>Shared fit signals</td><td colspan="2">${sameTags.length?sameTags.map(t=>pill(human(t))).join(''):'No shared editorial fit tags in the current maintained data.'}</td></tr><tr><td>Price context</td><td colspan="2">${esc(priceNote)}</td></tr></tbody></table></div></section><section class="section soft-panel"><p class="kicker">Which should you buy?</p><h2>Use the differentiators, not a generic winner badge.</h2><p>If one product's unique fit signals align with your stated priority and its compromise is acceptable, that is the stronger fit. If the choice is still unclear, use the <a href="/categories/${c.slug}/finder/">${esc(c.label)} Help Me Choose journey</a>.</p></section>`;return layout(req,{title:`${x.a.name} vs ${x.b.name} Australia | Which Fits You?`,description:`Compare ${x.a.name} and ${x.b.name} by use case, strengths, compromises, price context and retailer pathway.`,path,body,crumbs:[homeCrumb,{name:c.label,path:`/categories/${c.slug}/`},{name:'Compare',path:`/compare/${c.slug}/`},{name:`${x.a.name} vs ${x.b.name}`,path}]});}
function brandsIndex(req){const body=hero('Brand directory','Brands in the maintained catalogue','Browse the 16 brands currently represented in APG’s 37-product evidence set. Inclusion is editorial, not a partnership signal.')+`<section class="section"><div class="grid four">${brands.map(b=>{const list=products.filter(p=>p.brand===b),cats=[...new Set(list.map(p=>p.categoryLabel))];return `<article class="brand-card"><span class="brand-initial">${esc(b[0])}</span><h2><a href="/brands/${slugify(b)}/">${esc(b)}</a></h2><p>${list.length} maintained product${list.length===1?'':'s'}</p><div class="pills">${cats.map(c=>pill(c)).join('')}</div><a href="/brands/${slugify(b)}/" class="text-link">Browse ${esc(b)} →</a></article>`}).join('')}</div></section>`;return layout(req,{title:'Product Brands Australia | Australian Product Guide',description:'Browse brands represented in APG’s maintained Australian product comparison catalogue.',path:'/brands/',body,crumbs:[homeCrumb,{name:'Brands',path:'/brands/'}]});}
function brandPage(req,brand){const list=products.filter(p=>p.brand===brand),path=`/brands/${slugify(brand)}/`,cats=[...new Set(list.map(p=>p.categoryLabel))];const body=hero('Maintained brand coverage',brand,`${list.length} ${brand} product${list.length===1?' is':'s are'} currently maintained across ${cats.join(', ')}.`)+`<section class="section"><div class="notice">Brand inclusion is editorial and does not imply a partnership, endorsement or assessment of every product made by ${esc(brand)}.</div><div class="grid">${list.map(productCard).join('')}</div></section>`;return layout(req,{title:`${brand} Products Australia | Australian Product Guide`,description:`Maintained ${brand} products currently covered by Australian Product Guide, with comparisons and evidence links.`,path,body,crumbs:[homeCrumb,{name:'Brands',path:'/brands/'},{name:brand,path}]});}
function trustPage(req,slug){const p=trustContent[slug],path=`/${slug}/`,links=trust.map(s=>`<a href="/${s}/"${s===slug?' aria-current="page"':''}>${esc(trustContent[s].heading)}</a>`).join('');const body=hero('Trust, governance and transparency',p.heading,p.description)+`<section class="policy-shell"><nav class="policy-nav" aria-label="Trust and policy pages"><strong>Trust centre</strong>${links}</nav><article class="policy-content">${p.body}</article></section>`;return layout(req,{title:p.title,description:p.description,path,body,crumbs:[homeCrumb,{name:p.heading,path}]});}
function searchPage(req,u){const q=(u.searchParams.get('q')||'').trim(),r=searchSite(q);let results='';if(q){const intent=r.interpretation.length?`<div class="search-interpretation"><strong>APG interpreted:</strong> ${r.interpretation.map(x=>pill(x,'good')).join('')}</div>`:'',direct=r.directCompare?`<div class="direct-compare"><p class="eyebrow">Direct comparison detected</p><h2>${esc(r.directCompare.a.name)} vs ${esc(r.directCompare.b.name)}</h2><p>Open a two-product comparison using the exact maintained product records.</p><a class="button" href="${r.directCompare.url}">Compare these products</a></div>`:'';results=`${intent}${direct}<div class="search-groups">${r.products.length?`<section><div class="section-head"><div><p class="kicker">Products</p><h2>${r.products.length} relevant maintained product${r.products.length===1?'':'s'}</h2></div></div><div class="grid">${r.products.map(productCard).join('')}</div></section>`:''}${r.categories.length?`<section><div class="section-head"><div><p class="kicker">Categories</p><h2>Relevant decision hubs</h2></div></div><div class="category-grid">${r.categories.map(categoryCard).join('')}</div></section>`:''}${r.comparisons.length?`<section><div class="section-head"><div><p class="kicker">Comparisons</p><h2>Head-to-heads that may help</h2></div></div><div class="grid two">${r.comparisons.map(x=>`<article class="comparison-card"><h3><a href="${x.path}">${esc(x.a.name)} <span>vs</span> ${esc(x.b.name)}</a></h3></article>`).join('')}</div></section>`:''}${!r.products.length&&!r.categories.length&&!r.comparisons.length?`<div class="zero-state"><h2>No strong maintained match yet</h2><p>Try a category, brand, model or use case. APG deliberately does not invent results outside the current maintained catalogue.</p></div>`:''}</div>`;}const body=`<section class="search-hero"><div class="wrap"><p class="kicker">Product comparison search</p><h1>What are you trying to buy?</h1><p class="lede">Search by product, model, brand, use case or budget. APG translates the query into the current maintained catalogue.</p>${globalSearch(q)}<div class="suggested-queries"><span>Try:</span><a href="/search/?q=best+coffee+machine+under+%24800">coffee machine under A$800</a><a href="/search/?q=robot+vacuum+for+pet+hair">robot vacuum for pet hair</a><a href="/search/?q=Breville+vs+De%27Longhi">Breville vs De’Longhi</a></div></div></section><section class="section"><div class="wrap">${q?results:`<div class="grid four">${Object.values(categories).map(c=>`<a class="query-card" href="/search/?q=${encodeURIComponent(c.label)}"><strong>${esc(c.label)}</strong><span>${esc(c.description)}</span></a>`).join('')}</div>`}</div></section>`;return layout(req,{title:q?`${q} | Search | Australian Product Guide`:'Search Products | Australian Product Guide',description:'Search APG by product, brand, category, use case or budget.',path:'/search/',body,noindex:true,crumbs:[homeCrumb,{name:'Search',path:'/search/'}]});}
function sitemapHtml(req){const body=hero('Navigation','Sitemap','Browse every canonical indexable page in the maintained website.')+`<section class="section"><p><strong>${indexableRoutes.length} canonical indexable routes.</strong></p><ul class="pathway-list">${indexableRoutes.map(x=>`<li><a href="${x}">${esc(x)}</a></li>`).join('')}</ul></section>`;return layout(req,{title:'Sitemap | Australian Product Guide',description:'HTML navigation sitemap for Australian Product Guide.',path:'/sitemap/',body,noindex:true,crumbs:[homeCrumb,{name:'Sitemap',path:'/sitemap/'}]});}
function notFound(req){const body=hero('404','That page is not in the maintained guide','The URL may have changed, or the product/category may not be part of the current evidence set.')+`<section class="section"><div class="zero-state"><h2>Keep researching</h2>${globalSearch()}<div class="actions"><a class="button" href="/">Go home</a><a class="button secondary" href="/categories/">Browse categories</a></div></div></section>`;return layout(req,{title:'Page Not Found | Australian Product Guide',description:'The requested page could not be found.',path:'/404/',body,noindex:true});}
module.exports={home,categoriesIndex,categoryPage,pathwayPage,productPage,guidePage,finderPage,compareIndex,customCompare,pairPage,brandsIndex,brandPage,trustPage,searchPage,sitemapHtml,notFound};

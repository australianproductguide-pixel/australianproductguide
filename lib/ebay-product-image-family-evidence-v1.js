'use strict';

// APG audited family-level eBay image identities v1.0.
// These records are for APG products intentionally maintained at a product-family level rather than
// a single colour/SKU. Every entry is exact-item and title-bound. Entries contribute zero
// recommendation weight and do not relax category, accessory, variant, condition, voltage, price,
// active-listing, marketplace or image-host controls in the main exact-product guard.

module.exports=Object.freeze({
  'steelcase-series-2':Object.freeze({
    category:'office-chairs',
    itemId:'v1|398386409465|0',
    evidence:Object.freeze([]),
    titleOnly:true,
    titlePhrases:Object.freeze(['steelcase','series 2','ergonomic office chair','3d microknit'])
  })
});

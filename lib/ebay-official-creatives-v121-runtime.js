'use strict';

// APG official eBay Creative Gallery presentation v121.1.
// Premium retailer-discovery presentation only. Recommendation/evidence/eligibility/ranking stay untouched.
const creativeRegistry=require('../data/ebay-official-creatives-v121');
const ebay=require('../data/ebay-epn-interim-v1');
const VERSION='121.1';
const STYLE_HREF='/assets/ebay-official-creatives-v121.css?v=121.1';
const LEGACY_DISCOVERY=/<section class="section apg-amz-v41 apg-ebay-v11"[^>]*data-ebay-epn-discovery=[^>]*>[\s\S]*?<\/section>/i;
function escHtml(value){return String(value??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));}
function htmlHref(value){return String(value||'').replace(/&/g,'&amp;');}
function card(row){return `<a class="apg-ebay-official-v121-card" href="${htmlHref(row.destination)}" rel="sponsored nofollow noopener" target="_blank" data-affiliate-link data-affiliate-retailer="eBay Australia" data-affiliate-kind="official-creative-gallery" data-affiliate-placement="ebay_official_creative_${escHtml(row.key)}" data-ebay-official-creative="${escHtml(row.key)}" data-ebay-exact-model="false"><span class="apg-ebay-official-v121-media"><img class="apg-ebay-official-v121-image" src="${escHtml(row.image)}" width="700" height="400" loading="lazy" decoding="async" alt="${escHtml(row.alt)}"></span><span class="apg-ebay-official-v121-copy"><span class="apg-ebay-official-v121-label">Official eBay creative</span><strong>${escHtml(row.title)}</strong><span>Browse on eBay Australia <span aria-hidden="true">↗</span></span></span></a>`;}
function promotionCard(row){return `<a class="apg-ebay-official-v121-promo" href="${htmlHref(row.url)}" rel="sponsored nofollow noopener" target="_blank" data-affiliate-link data-affiliate-retailer="eBay Australia" data-affiliate-kind="${escHtml(row.kind||'collection')}" data-affiliate-placement="ebay_discovery_${escHtml(row.key)}" data-ebay-epn-collection="${escHtml(row.key)}" data-ebay-exact-model="false"><span><strong>${escHtml(row.title)}</strong><small>${escHtml(row.description)}</small></span><span class="apg-ebay-official-v121-promo-cta">Browse <span aria-hidden="true">↗</span></span></a>`;}
function section(rows,{primary=false}={}){if(!rows.length)return '';const promo=primary?ebay.promotionRows().map(promotionCard).join(''):'';const heading=primary?'Shop eBay Australia with official creative':'Browse a relevant eBay Australia category';const intro=primary?'Official eBay Creative Gallery assets are used as retailer-discovery aids. These paid pathways remain separate from APG recommendations and contribute zero recommendation points.':'This paid retailer-discovery pathway is relevant to this category and remains separate from APG recommendations.';return `<section class="apg-ebay-official-v121${primary?'':' apg-ebay-official-v121-compact'}" data-ebay-official-creatives-v121="true" data-ebay-official-version="${VERSION}" aria-labelledby="apgEbayOfficialV121Title"><div class="apg-ebay-official-v121-head"><div><p class="kicker">eBay Australia discovery</p><h2 id="apgEbayOfficialV121Title">${heading}</h2><p>${intro}</p></div><span class="apg-ebay-official-v121-badge">Official eBay creative</span></div><div class="apg-ebay-official-v121-grid">${rows.map(card).join('')}</div>${promo?`<div class="apg-ebay-official-v121-secondary"><div class="apg-ebay-official-v121-secondary-head"><strong>More eBay Australia shopping pathways</strong><span>Current collection and refurbished discovery links</span></div><div class="apg-ebay-official-v121-promos">${promo}</div></div>`:''}<div class="apg-ebay-official-v121-disclosure"><strong>Paid eBay Australia links.</strong> APG may earn a commission from qualifying purchases. Retailer participation and commission do not affect suitability, ranking or recommendations.</div><p class="apg-ebay-official-v121-fine">Creative and collection links open eBay Australia discovery results or collection pages. APG does not verify an exact listing, live price, stock, seller, condition grade or warranty from these promotional pathways. Confirm the exact item and current details on eBay before buying.</p></section>`;}
function ensureStyle(html){const out=String(html||'');if(out.includes('/assets/ebay-official-creatives-v121.css'))return out;return out.replace(/<\/head>/i,`<link rel="stylesheet" href="${STYLE_HREF}"></head>`);}
function replaceHomeLegacyDiscovery(out,block){if(LEGACY_DISCOVERY.test(out))return out.replace(LEGACY_DISCOVERY,`<section class="section apg-ebay-premium-v121-shell"><div class="wrap">${block}</div></section>`);return out.replace(/<\/main>/i,`<section class="section apg-ebay-premium-v121-shell"><div class="wrap">${block}</div></section></main>`);}
function inject(html,path){
  let out=String(html||'');if(!out||!/<html|<!doctype/i.test(out)||out.includes('data-ebay-official-creatives-v121="true"'))return out;
  // Deals is owned by Smart Placement v1.6. Remove the superseded icon-art EPN discovery section
  // so the newer marketplace spotlight is the only eBay visual presentation on that route.
  if(path==='/deals/')return out.replace(LEGACY_DISCOVERY,'');
  const rows=creativeRegistry.forPath(path);if(!rows.length)return out;out=ensureStyle(out);
  if(path==='/')return replaceHomeLegacyDiscovery(out,section(rows,{primary:true}));
  return out.replace(/<\/main>/i,`<section class="section apg-ebay-premium-v121-shell"><div class="wrap">${section(rows,{primary:false})}</div></section></main>`);
}
function wrap(downstream){if(typeof downstream!=='function')throw new TypeError('eBay official creative wrapper requires downstream handler');function handler(req,res){let path='/';try{path=new URL(req.url,'https://australianproductguide.au').pathname}catch{}const contentType=()=>String((typeof res.getHeader==='function'&&res.getHeader('Content-Type'))||'');const originalEnd=res.end.bind(res);const originalWrite=typeof res.write==='function'?res.write.bind(res):null;const chunks=[];if(originalWrite)res.write=function(chunk,encoding,cb){if(chunk)chunks.push(Buffer.isBuffer(chunk)?chunk:Buffer.from(String(chunk),encoding));if(typeof cb==='function')cb();return true;};res.end=function(chunk,encoding,cb){if(chunk)chunks.push(Buffer.isBuffer(chunk)?chunk:Buffer.from(String(chunk),encoding));if(!chunks.length)return originalEnd(chunk,encoding,cb);const body=Buffer.concat(chunks).toString('utf8');const isHtml=/text\/html/i.test(contentType())||/<html|<!doctype/i.test(body);const next=isHtml?inject(body,path):body;if(typeof res.setHeader==='function')res.setHeader('X-APG-eBay-Official-Creatives','v'+VERSION);return originalEnd(next,'utf8',cb);};return downstream(req,res);}Object.assign(handler,downstream,{EBAY_OFFICIAL_CREATIVES_VERSION:VERSION,transformEbayOfficialCreatives:inject});return handler;}
module.exports={VERSION,STYLE_HREF,LEGACY_DISCOVERY,card,promotionCard,section,ensureStyle,replaceHomeLegacyDiscovery,inject,wrap};

name: My APG account journey v24.1 QA

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  account-journey-v241:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Syntax check
        run: |
          node --check lib/account-profile-v24.js
          node --check public/assets/account-journey-v241.js
          node --check scripts/account-journey-v241-qa.js
      - name: Run journey regression QA
        run: node scripts/account-journey-v241-qa.js

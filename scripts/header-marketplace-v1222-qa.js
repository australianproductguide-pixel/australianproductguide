'use strict';
const assert=require('node:assert/strict');
const fs=require('node:fs');
const path=require('node:path');
const app=require('../api/index');
const header=require('../lib/header-marketplace-v1222-runtime');
function render(url,method='GET'){return new Promise((resolve,reject)=>{const headers={};const req={url,method,headers:{host:'australianproductguide.au'},on(){},destroy(){}};const chunks=[];const res={statusCode:200,setHeader(k,v){headers[String(k).toLowerCase()]=String(v)},getHeader(k){return headers[String(k).toLowerCase()]},removeHeader(k){delete headers[String(k).toLowerCase()]},write(body=''){chunks.push(Buffer.isBuffer(body)?body.toString('utf8'):String(body||''));return true},end(body=''){chunks.push(Buffer.isBuffer(body)?body.toString('utf8'):String(body||''));resolve({status:this.statusCode,headers,body:chunks.join('')})}};try{const result=app(req,res);if(result&&typeof result.then==='function')result.catch(reject)}catch(error){reject(error)}})}
function count(text,needle){return String(text).split(needle).length-1}
(async()=>{
  assert.equal(header.VERSION,'122.2');assert.equal(header.CSS_PATH,'/assets/header-marketplace-v1222.css');
  const routes=['/','/categories/coffee-machines/','/products/breville-barista-express-impress-bes876/','/decision-lab/','/compare/'];
  for(const route of routes){const response=await render(route);assert.equal(response.status,200,`${route} must render`);assert.equal(response.headers['x-apg-header-marketplace-mobile-ownership'],'v122.2');assert.equal(response.headers['x-apg-pagespeed-runtime-css'],'P0_DISABLED_RECURSIVE_CAPTURE');assert.equal(count(response.body,'name="apg-header-marketplace-mobile-ownership"'),1);assert.equal(count(response.body,'/assets/header-marketplace-v1222.css?v=122.2'),1,`${route} must directly include v122.2 during P0 safe cascade`);assert.equal(count(response.body,'data-apg-mobile-account-v122'),1);assert(response.body.includes('data-apg-search-category'));assert(response.body.includes('class="mobile-toggle"'));if(route==='/'){assert(!response.body.includes('/assets/pagespeed-home-v113.css?'));assert(response.body.includes('name="apg-pagespeed-runtime-css" content="P0_DISABLED_RECURSIVE_CAPTURE"'));}}
  const asset=await render('/assets/header-marketplace-v1222.css?v=122.2');assert.equal(asset.status,200);assert.equal(asset.headers['content-type'],'text/css; charset=utf-8');assert.equal(asset.headers['x-apg-header-marketplace-mobile-ownership'],'v122.2');for(const token of ['grid-template-columns:44px minmax(0,1fr) 44px!important','grid-template-rows:48px 52px!important','grid-template-areas:"menu brand account" "search search search"!important','.site-header .masthead>.apg-mobile-account-v122>span{display:none!important','grid-area:search!important;grid-column:1/-1!important;grid-row:2!important','width:100%!important;max-width:none!important;min-width:0!important;height:44px!important','border-radius:0 9px 9px 0!important','linear-gradient(180deg,var(--apg1222-gold-top),var(--apg1222-gold-bottom))!important','grid-template-columns:minmax(0,1fr) 56px!important'])assert(asset.body.includes(token),`v122.2 CSS must retain ${token}`);
  const source=fs.readFileSync(path.join(__dirname,'..','lib','header-marketplace-v1222-runtime.js'),'utf8');for(const banned of ['scoreProduct(','rankDecision(','commissionWeight','localStorage.setItem(','sessionStorage.setItem('])assert(!source.includes(banned));
  console.log(`HEADER_MARKETPLACE_V1222=PASS routes=${routes.length} mobileAccountText=zero mobileBrand=full mobileSearch=row2 desktopSearch=premium recommendationWeight=0 homepageCss=direct-p0-safe`);
})().catch(error=>{console.error(error&&error.stack||error);process.exit(1)});
